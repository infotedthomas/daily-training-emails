{
  "name": "training-reminders",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "upload-template": "node scripts/upload-template.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
